{
  "name": "zwu-unlocker-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.23",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.18.0"
  }
}
